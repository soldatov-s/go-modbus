// rest
package modbus

/*
import (
	"encoding/json"
	"fmt"
	"math/rand"
	"net/http"
	"strconv"

	"github.com/gorilla/mux"
)

type ModbusRESTreq struct {
	id   int `json:"id"`
	fc   int `json:"fc"`
	addr int `json:"addr"`
	cnt  int `json:"cnt"`
}

func CreateRest() {
	r := mux.NewRouter()
	r.HandleFunc("/read", readModbusData).Methods("GET")
	r.HandleFunc("/books", writeModbusData).Methods("POST")
	http.ListenAndServe(":8000", r)
}
*/
